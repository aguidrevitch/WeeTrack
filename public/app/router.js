define([
    // Application.
    "app",

    // Modules
    "modules/home",
    "modules/user"
    ],

    function(app) {

        var Router = Backbone.Router.extend({});
        return Router;

    });
